{
  "name": "osegbe-lab-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "sync": "node scripts/sync-labs.mjs --apply"
  },
  "dependencies": {
    "@sanity/client": "8.6.1",
    "sharp": "0.35.4"
  }
}
